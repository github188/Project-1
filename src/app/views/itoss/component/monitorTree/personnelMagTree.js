var React = require('react');var ReactRouter = require('react-router');var Router = ReactRouter.Router;var Route = ReactRouter.Route;var History = ReactRouter.History;var Navigation = require('react-router').Navigation;import Store from '../../../../server/store';var ZtreeviewPer = React.createClass({    //mixins: [History],    componentWillMount:function(){        this.props.get_unitData(this);    },//{id:1,name:"资产管理",pid:0,open:true},    initTree:function(treeData){        console.log(treeData);        var zTree;        var that = this;        var check = {            enable: true,            //是否无法选择            chkDisabledInherit: true,            //是否单选框            chkStyle: "checkbox"        };        var setting = {            view: {                dblClickExpand: false,                showLine: false,                nameIsHTML: true,                showIcon: false,                selectedMulti: false            },            data: {                simpleData: {                    enable:true,                    idKey: "id",                    pIdKey: "pid",                    rootPid: ""                },                key:{                    name:"name"                }            },            check: check,            callback: {                beforeClick: function(treeId, treeNode) {                    that.onClickChild(treeNode);                },                onCheck :function(event,treeId,treeNode){                    that.onCheckTree(event,treeId,treeNode);                }            }        };        $(document).ready(function(){            var t = $("#personnelMagTree");            t = $.fn.zTree.init(t, setting, treeData);            var zTree = $.fn.zTree.getZTreeObj("personnelMagTree");        });    },    componentDidMount:function(){        this.initTree();    },    getChecked:function(){        var zTree = $.fn.zTree.getZTreeObj("personnelMagTree");        var nodes = zTree.getCheckedNodes(true);        return nodes;    },    onClickChild:function(treeNode){        var zTree = $.fn.zTree.getZTreeObj("personnelMagTree");        var checkedState = treeNode.checked;        if(checkedState){            checkedState = false;        }else{            checkedState = true;        };        zTree.checkNode(treeNode,checkedState,true);        var checkedList = zTree.getCheckedNodes(true);        var curNode1 = this.state.curNode;        this.setState({curNode:treeNode});    },    onCheckTree:function(event,treeId,treeNode){        var zTree = $.fn.zTree.getZTreeObj("personnelMagTree");        var checkedList = zTree.getCheckedNodes(true);    },    render:function(){        return(            <div className="zTreeMonitor personnelMagTree">                <ul id="personnelMagTree" className="ztree" style={{"backgroundColor":"#fff"}}></ul>            </div>        );    }});module.exports = ZtreeviewPer;